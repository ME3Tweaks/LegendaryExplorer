namespace LegendaryExplorer.SharedUI
{
    /// <summary>
    /// Class that holds a Name, and the index of the name in the name table
    /// </summary>
    public sealed record IndexedName(int Index, string Name);
}
