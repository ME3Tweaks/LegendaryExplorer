<UserControl x:Class="LegendaryExplorer.Tools.PathfindingNetworkEditor.Controls.NodeInfoPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:LegendaryExplorer.Tools.PathfindingNetworkEditor.Controls"
             xmlns:converters="clr-namespace:LegendaryExplorer.SharedUI.Converters"
             mc:Ignorable="d"
             d:DesignHeight="500" d:DesignWidth="300"
             DataContext="{Binding RelativeSource={RelativeSource Self}}">
    <UserControl.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <converters:InverseBoolToVisibilityConverter x:Key="InverseBoolToVisibilityConverter"/>
        <converters:NullVisibilityConverter x:Key="NullVisibilityConverter"/>
        <DataTemplate x:Key="CoverSlotDetailTemplate">
            <Grid Margin="0,2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Grid.Column="0" Text="Cover Type:" FontWeight="Bold" Margin="0,1,6,1"/>
                <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding CoverTypeText}" Margin="0,1"/>
                <!--<TextBlock Grid.Row="1" Grid.Column="0" Text="Description:" FontWeight="Bold" Margin="0,1,6,1"/>
                <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding LocationDescriptionText}" Margin="0,1"/> -->
                <TextBlock Grid.Row="2" Grid.Column="0" Text="Actions:" FontWeight="Bold" Margin="0,1,6,1"/>
                <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding ActionsText}" TextWrapping="Wrap" Margin="0,1"/>
                <TextBlock Grid.Row="3" Grid.Column="0" Text="Can:" FontWeight="Bold" Margin="0,1,6,1"/>
                <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding CapabilityFlagsText}" TextWrapping="Wrap" Margin="0,1"/>
                <TextBlock Grid.Row="4" Grid.Column="0" Text="Allow:" FontWeight="Bold" Margin="0,1,6,1"/>
                <TextBlock Grid.Row="4" Grid.Column="1" Text="{Binding AllowFlagsText}" TextWrapping="Wrap" Margin="0,1"/>
                <TextBlock Grid.Row="5" Grid.Column="0" Text="Offset:" FontWeight="Bold" Margin="0,1,6,1"/>
                <TextBlock Grid.Row="5" Grid.Column="1" Text="{Binding LocationOffsetText}" Margin="0,1"/>
                <TextBlock Grid.Row="6" Grid.Column="0" Text="Links:" FontWeight="Bold" Margin="0,1,6,1"/>
                <TextBlock Grid.Row="6" Grid.Column="1" Text="{Binding LinksText}" TextWrapping="Wrap" Margin="0,1"/>
            </Grid>
        </DataTemplate>
    </UserControl.Resources>
    <Grid MinWidth="280">
        <TextBlock Text="No node selected" HorizontalAlignment="Center" VerticalAlignment="Center"
                   Foreground="Gray"
                   Visibility="{Binding HasNode, Converter={StaticResource InverseBoolToVisibilityConverter}}"/>
        <ScrollViewer VerticalScrollBarVisibility="Auto"
                      Visibility="{Binding HasNode, Converter={StaticResource BoolToVisibilityConverter}}">
            <StackPanel Margin="6,4">
                <TextBlock Text="{Binding NodeTitle}" FontWeight="Bold" FontSize="13"
                           TextWrapping="Wrap" Margin="0,0,0,4"/>

                <!-- General -->
                <GroupBox Header="General" Padding="4,0,4,4">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <TextBlock Grid.Row="0" Grid.Column="0" Text="Class:" FontWeight="Bold" Margin="0,1,6,1"/>
                        <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding ClassName}" TextWrapping="Wrap" Margin="0,1"/>
                        <TextBlock Grid.Row="1" Grid.Column="0" Text="Package:" FontWeight="Bold" Margin="0,1,6,1"/>
                        <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding PackageName}" TextWrapping="Wrap" Margin="0,1"/>
                        <TextBlock Grid.Row="2" Grid.Column="0" Text="Path:" FontWeight="Bold" Margin="0,1,6,1"/>
                        <TextBlock Grid.Row="2" Grid.Column="1" TextWrapping="Wrap" Margin="0,1">
                            <Hyperlink Command="{Binding NavigateToExportCommand}">
                                <Run Text="{Binding FullPath, Mode=OneWay}"/>
                            </Hyperlink>
                        </TextBlock>
                        <TextBlock Grid.Row="3" Grid.Column="0" Text="Position:" FontWeight="Bold" Margin="0,1,6,1"/>
                        <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding PositionText}" Margin="0,1"/>
                        <TextBlock Grid.Row="4" Grid.Column="0" Text="Collision:" FontWeight="Bold" Margin="0,1,6,1"/>
                        <TextBlock Grid.Row="4" Grid.Column="1" Text="{Binding CollisionText}" Margin="0,1"/>
                    </Grid>
                </GroupBox>

                <!-- Reach Specs -->
                <GroupBox Header="{Binding ReachSpecsHeader}" Padding="4,0,4,4" Margin="0,4,0,0">
                    <ItemsControl ItemsSource="{Binding ReachSpecItems}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border BorderBrush="#3A3A3A" BorderThickness="0,0,0,1" Padding="0,3,0,3">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>
                                        <TextBlock Grid.Row="0" Grid.Column="0" Text="Type:" FontWeight="Bold" Margin="0,1,6,1"/>
                                        <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding SpecType}" TextWrapping="Wrap" Margin="0,1"/>
                                        <TextBlock Grid.Row="1" Grid.Column="0" Text="Target:" FontWeight="Bold" Margin="0,1,6,1"/>
                                        <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding Target}" TextWrapping="Wrap" Margin="0,1"/>
                                        <TextBlock Grid.Row="2" Grid.Column="0" Text="Size:" FontWeight="Bold" Margin="0,1,6,1"/>
                                        <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding SizeText}" Margin="0,1"/>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </GroupBox>

                <!-- Cover Slots (CoverLink only) -->
                <GroupBox Header="{Binding CoverSlotsHeader}" Padding="4,0,4,4" Margin="0,4,0,0"
                          Visibility="{Binding IsCoverLink, Converter={StaticResource BoolToVisibilityConverter}}">
                    <ItemsControl ItemsSource="{Binding CoverSlotItems}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Expander Margin="0,2">
                                    <Expander.Header>
                                        <TextBlock Text="{Binding Header}" FontWeight="SemiBold"/>
                                    </Expander.Header>
                                    <ContentControl Content="{Binding}"
                                                    ContentTemplate="{StaticResource CoverSlotDetailTemplate}"
                                                    Margin="8,2,0,4"/>
                                </Expander>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </GroupBox>

                <!-- Owning Slot (CoverSlotMarker only) -->
                <GroupBox Header="Owning Cover Slot" Padding="4,0,4,4" Margin="0,4,0,0"
                          Visibility="{Binding IsCoverSlotMarker, Converter={StaticResource BoolToVisibilityConverter}}">
                    <StackPanel>
                        <TextBlock Text="{Binding OwningCoverLinkInfo}" FontStyle="Italic"
                                   TextWrapping="Wrap" Margin="0,0,0,4"/>
                        <ContentControl Content="{Binding OwningSlotItem}"
                                        ContentTemplate="{StaticResource CoverSlotDetailTemplate}"
                                        Visibility="{Binding OwningSlotItem, Converter={StaticResource NullVisibilityConverter}}"/>
                        <TextBlock Text="Owning slot not yet resolved" Foreground="Orange"
                                   Visibility="{Binding OwningSlotItem, Converter={StaticResource NullVisibilityConverter}, ConverterParameter=Reversed}"/>
                    </StackPanel>
                </GroupBox>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
